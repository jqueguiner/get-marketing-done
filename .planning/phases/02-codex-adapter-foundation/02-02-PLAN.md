---
phase: 02-codex-adapter-foundation
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - /home/ubuntu/marketing/scripts/marketing-tools.js
  - /home/ubuntu/marketing/bin/install.js
  - /home/ubuntu/marketing/skills
  - /home/ubuntu/marketing/README.md
  - /home/ubuntu/marketing/scripts/verify_codex_command_sweep.js
autonomous: true
requirements: ["ADPT-01"]

must_haves:
  truths:
    - "Codex adapter is wired through runtime ingress, installer flow, and skill registration paths."
    - "Every exposed `$gmd-*` command is validated through a full command sweep."
    - "Codex command execution path reaches canonical actions end-to-end for stage commands."
  artifacts:
    - path: "/home/ubuntu/marketing/scripts/marketing-tools.js"
      provides: "Runtime ingress codex adapter execution path"
      min_lines: 200
    - path: "/home/ubuntu/marketing/bin/install.js"
      provides: "Installer wiring for codex command surface"
      min_lines: 200
    - path: "/home/ubuntu/marketing/scripts/verify_codex_command_sweep.js"
      provides: "Automated full-sweep validator for `$gmd-*` command set"
      min_lines: 60
    - path: "/home/ubuntu/marketing/README.md"
      provides: "Codex command usage and validation coverage notes"
      contains: "$gmd-"
  key_links:
    - from: "bin/install.js"
      to: "skills/"
      via: "codex command registration path"
      pattern: "gmd|codex"
    - from: "verify_codex_command_sweep.js"
      to: "providers/codex.js"
      via: "command inventory source of truth"
      pattern: "commandMap"
    - from: "verify_codex_command_sweep.js"
      to: "marketing-tools.js"
      via: "runtime execution checks"
      pattern: "scripts/marketing-tools.js"
---

<objective>
Wire the Codex adapter end-to-end across runtime, installer, and skill registration surfaces, then prove coverage with a full `$gmd-*` command sweep.

Purpose: This closes ADPT-01 by ensuring Codex adapter behavior is not partial and is validated across all exposed native commands.
Output: Full wiring updates plus automated sweep verification artifact and updated operator docs.
</objective>

<execution_context>
@/home/ubuntu/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/ubuntu/marketing/.planning/PROJECT.md
@/home/ubuntu/marketing/.planning/ROADMAP.md
@/home/ubuntu/marketing/.planning/REQUIREMENTS.md
@/home/ubuntu/marketing/.planning/STATE.md
@/home/ubuntu/marketing/.planning/phases/02-codex-adapter-foundation/02-CONTEXT.md
@/home/ubuntu/marketing/.planning/phases/02-codex-adapter-foundation/02-RESEARCH.md
@/home/ubuntu/marketing/.planning/phases/02-codex-adapter-foundation/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete codex runtime + installer + skill wiring</name>
  <files>/home/ubuntu/marketing/scripts/marketing-tools.js, /home/ubuntu/marketing/bin/install.js, /home/ubuntu/marketing/skills</files>
  <behavior>
    - Codex execution paths are wired in runtime ingress.
    - Installer output includes codex-facing command registration.
    - Skill registration surface reflects codex native command inventory.
  </behavior>
  <action>
Update `scripts/marketing-tools.js` where needed to ensure codex provider mode executes stage commands through routed canonical actions with strict-native behavior from Plan 02-01.

Update `bin/install.js` to include codex command registration/wiring outputs aligned to `$gmd-*` surface. Preserve existing install/update behavior and file manifest guarantees.

Update relevant files under `skills/` so command references and registration metadata support codex native command surface consistently.

Do not broaden scope to non-codex provider refactors in this phase.
  </action>
  <verify>
<automated>
node scripts/marketing-tools.js init-campaign >/dev/null && node -e "const fs=require('fs'); const p=fs.readFileSync('./bin/install.js','utf8'); console.log(/gmd|codex/i.test(p));"
</automated>
  </verify>
  <done>
Codex adapter is wired through runtime ingress and installation/skill registration surfaces with consistent `$gmd-*` command exposure.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement full codex command sweep validator</name>
  <files>/home/ubuntu/marketing/scripts/verify_codex_command_sweep.js</files>
  <behavior>
    - Discovers full `$gmd-*` command inventory from codex provider map.
    - Executes routing/runtime checks for each command.
    - Fails non-zero if any command is unmapped/broken; outputs clear report.
  </behavior>
  <action>
Create `scripts/verify_codex_command_sweep.js` (Node, stdlib/CommonJS) that:
- Loads codex provider map from `scripts/adapters/providers/codex.js`.
- Iterates all `$gmd-*` commands.
- For each command:
  - calls router to ensure canonical mapping works,
  - runs lightweight runtime command validation path (safe command form, no destructive operations).
- Produces summary JSON with pass/fail per command and overall status.
- Exits code 1 on any failure.

Include safeguards so sweep uses read-only or initialization-safe flows where possible.
  </action>
  <verify>
<automated>
node scripts/verify_codex_command_sweep.js
</automated>
  </verify>
  <done>
Full codex command sweep is automated and enforceable as phase exit gate.
  </done>
</task>

<task type="auto">
  <name>Task 3: Document codex native command coverage and sweep process</name>
  <files>/home/ubuntu/marketing/README.md</files>
  <action>
Update README to document:
- Codex-native command surface is `$gmd-*` only.
- Unmapped codex native commands hard-fail by design.
- Full command sweep command (`node scripts/verify_codex_command_sweep.js`) and expected usage.

Keep previous compatibility notes; align wording with strict Phase 2 decisions.
  </action>
  <verify>
<automated>
rg -n "\$gmd-|hard-fail|verify_codex_command_sweep" README.md
</automated>
  </verify>
  <done>
README reflects strict codex native model and full-sweep verification flow.
  </done>
</task>

</tasks>

<verification>
1. Installer/skills/runtime show codex wiring paths.
2. Full command sweep passes for all exposed `$gmd-*` commands.
3. Sweep fails appropriately when mapping gaps are introduced (sanity tested during implementation).
4. Documentation matches strict-native and hard-fail behavior.
</verification>

<success_criteria>
- ADPT-01 satisfied with end-to-end codex adapter execution coverage.
- Adapter behavior remains isolated in adapter/runtime integration surfaces.
- Full command sweep is reproducible and required for phase completion.
</success_criteria>

<output>
After completion, create `.planning/phases/02-codex-adapter-foundation/02-02-SUMMARY.md`
</output>
