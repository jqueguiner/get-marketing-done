---
phase: 02-codex-adapter-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/ubuntu/marketing/scripts/adapters/providers/codex.js
  - /home/ubuntu/marketing/scripts/adapters/command-router.js
  - /home/ubuntu/marketing/scripts/adapters/canonical-actions.js
autonomous: true
requirements: ["ADPT-01"]

must_haves:
  truths:
    - "Codex native commands are accepted only in `$gmd-*` format."
    - "Unmapped codex native commands fail immediately with clear structured error."
    - "Codex native command parsing maps to canonical actions without fallback shims."
  artifacts:
    - path: "/home/ubuntu/marketing/scripts/adapters/providers/codex.js"
      provides: "Strict codex native command inventory and mappings"
      min_lines: 40
    - path: "/home/ubuntu/marketing/scripts/adapters/command-router.js"
      provides: "Codex-native parser gate and hard-fail behavior"
      min_lines: 80
    - path: "/home/ubuntu/marketing/scripts/adapters/canonical-actions.js"
      provides: "Canonical action coverage for mapped codex command set"
      min_lines: 80
  key_links:
    - from: "providers/codex.js"
      to: "command-router.js"
      via: "routeCommand codex native map lookup"
      pattern: "\\$gmd-"
    - from: "command-router.js"
      to: "canonical-actions.js"
      via: "assertValidAction and normalizeActionPayload"
      pattern: "assertValidAction|normalizeActionPayload"
---

<objective>
Implement the strict Codex adapter parser and translator path with no ambiguity: `$gmd-*` only, hard-fail for unmapped native commands, and canonical action mapping coverage.

Purpose: This is the foundation requirement for ADPT-01. It locks native command semantics before broader runtime/install integration.
Output: Updated Codex provider map and router behavior enforcing strict-native and hard-fail constraints.
</objective>

<execution_context>
@/home/ubuntu/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/ubuntu/marketing/.planning/PROJECT.md
@/home/ubuntu/marketing/.planning/ROADMAP.md
@/home/ubuntu/marketing/.planning/REQUIREMENTS.md
@/home/ubuntu/marketing/.planning/STATE.md
@/home/ubuntu/marketing/.planning/phases/02-codex-adapter-foundation/02-CONTEXT.md
@/home/ubuntu/marketing/.planning/phases/02-codex-adapter-foundation/02-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden Codex provider map to strict native command set</name>
  <files>/home/ubuntu/marketing/scripts/adapters/providers/codex.js, /home/ubuntu/marketing/scripts/adapters/canonical-actions.js</files>
  <behavior>
    - Codex provider map contains only `$gmd-*` native keys.
    - All mapped codex commands resolve to valid canonical action IDs.
    - Unsupported or alias-style command keys are not defined as native codex entries.
  </behavior>
  <action>
Update `scripts/adapters/providers/codex.js` so native command keys are strictly `$gmd-*` entries only. Ensure key command coverage includes progress, verify, pause/resume, context init, list/research/enrichment/segmentation/email/outreach flows represented in current product surface.

Update `scripts/adapters/canonical-actions.js` if any required canonical action identifiers are missing for the codex mapping set. Do not add provider-specific data to canonical contract.

Keep module format CommonJS and preserve exported shape `{ provider, commandMap }`.
  </action>
  <verify>
<automated>
node -e "const p=require('./scripts/adapters/providers/codex.js'); const keys=Object.keys(p.commandMap); console.log(keys.every(k=>k.startsWith('$gmd-'))); const c=require('./scripts/adapters/canonical-actions.js'); console.log(keys.length>10 && keys.every(k=>Object.values(c.ACTIONS).includes(p.commandMap[k])));"
</automated>
  </verify>
  <done>
Codex provider map is strict-native (`$gmd-*` only) and every mapped value is a valid canonical action.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce codex native parser gate and hard-fail behavior</name>
  <files>/home/ubuntu/marketing/scripts/adapters/command-router.js</files>
  <behavior>
    - Codex native mode accepts only `$gmd-*` for native parsing.
    - Unmapped codex native command throws clear, structured hard-fail error.
    - No alias fallback or compatibility shim applies when codex native command is unmapped.
  </behavior>
  <action>
Update `scripts/adapters/command-router.js` routing rules:
- Introduce helper to identify codex-native commands (`$gmd-*`).
- For provider `codex` and native-form command:
  - if mapped: route normally.
  - if unmapped: throw an error (`UNKNOWN_CODEX_COMMAND`) with clear message and include command/provider context.
- Keep canonical alias handling available only for explicit alias inputs and alias-enabled mode; do not silently use alias fallback for failed codex-native mapping.
- Preserve native-precedence conflict behavior when both native and alias candidates exist.

Ensure returned payload remains normalized and free of secret metadata fields.
  </action>
  <verify>
<automated>
node -e "const r=require('./scripts/adapters/command-router.js'); const ok=r.routeCommand({provider:'codex',command:'$gmd-campaign-progress'}); console.log(ok.action==='campaign.progress'); try{r.routeCommand({provider:'codex',command:'$gmd-not-real'}); console.log(false);}catch(e){console.log(e.code==='UNKNOWN_CODEX_COMMAND');}"
</automated>
  </verify>
  <done>
Command router strictly enforces codex-native parsing and hard-fails unmapped `$gmd-*` commands with clear error code/message.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add codex adapter contract checks for strict-native guarantees</name>
  <files>/home/ubuntu/marketing/scripts/adapters/command-router.js, /home/ubuntu/marketing/scripts/adapters/providers/codex.js</files>
  <action>
Add lightweight self-check helpers (or exported diagnostics) that allow full-sweep scripts to inspect codex provider map and strict-native behavior without duplicating logic. Keep these checks minimal and non-invasive.

Ensure diagnostics can list supported codex-native commands and identify strict-native enforcement mode.
  </action>
  <verify>
<automated>
node -e "const r=require('./scripts/adapters/command-router.js'); const cmds=r.listSupportedCommands('codex'); console.log(Array.isArray(cmds) && cmds.length>10 && cmds.every(c=>c.startsWith('$gmd-')));"
</automated>
  </verify>
  <done>
Codex strict-native guarantees are introspectable for downstream sweep validation in Plan 02.
  </done>
</task>

</tasks>

<verification>
1. Codex native map contains only `$gmd-*` keys.
2. Mapped codex commands resolve to canonical actions.
3. Unmapped codex native command throws hard-fail error (`UNKNOWN_CODEX_COMMAND`).
4. No alias fallback path is taken for unmapped codex-native commands.
</verification>

<success_criteria>
- ADPT-01 foundation established via strict codex parser + translator behavior.
- Hard-fail semantics are deterministic and testable.
- Adapter-specific behavior remains isolated to adapter layer files.
</success_criteria>

<output>
After completion, create `.planning/phases/02-codex-adapter-foundation/02-01-SUMMARY.md`
</output>
