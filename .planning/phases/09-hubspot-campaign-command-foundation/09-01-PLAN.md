# Plan 09-01: HubSpot Campaign Command Routing

## Objective
Add provider-native and canonical routing for HubSpot campaign operations.

## Tasks
1. Add canonical action mapping for `hubspot.campaign`.
2. Add provider-native command declarations for Claude and Codex.
3. Route command through shared runtime dispatcher.

## Exit Criteria
- Native commands resolve with no router errors.
- Codex command sweep includes `$gmd-hubspot-campaign` and passes.
