---
phase: 07-additional-adapter-scaffolds
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/ubuntu/marketing/scripts/adapters/providers/gemini.js
  - /home/ubuntu/marketing/scripts/adapters/providers/opencode.js
  - /home/ubuntu/marketing/scripts/adapters/providers/mistral.js
  - /home/ubuntu/marketing/scripts/adapters/command-router.js
  - /home/ubuntu/marketing/scripts/adapters/capability-matrix.js
  - /home/ubuntu/marketing/scripts/marketing-tools.js
  - /home/ubuntu/marketing/config.example.json
  - /home/ubuntu/marketing/bin/install.js
autonomous: true
requirements: ["ADPT-04", "ADPT-05", "ADPT-06"]

must_haves:
  truths:
    - "Gemini/OpenCode/Mistral scaffold providers are registered with minimal native command maps mapped to canonical actions."
    - "Scaffold providers are inactive by default and require explicit config activation."
    - "Unsupported scaffold capability paths emit structured diagnostics with code, capability key, and remediation hint."
  artifacts:
    - path: "/home/ubuntu/marketing/scripts/adapters/providers/gemini.js"
      provides: "Gemini scaffold provider command map"
      min_lines: 30
    - path: "/home/ubuntu/marketing/scripts/adapters/providers/opencode.js"
      provides: "OpenCode scaffold provider command map"
      min_lines: 30
    - path: "/home/ubuntu/marketing/scripts/adapters/providers/mistral.js"
      provides: "Mistral scaffold provider command map"
      min_lines: 30
    - path: "/home/ubuntu/marketing/scripts/adapters/command-router.js"
      provides: "Scaffold provider registration + activation-gate diagnostics"
      min_lines: 130
  key_links:
    - from: "providers/*.js"
      to: "canonical-actions.js"
      via: "minimal command map to shared action contract"
      pattern: "ACTIONS"
    - from: "command-router.js"
      to: "capability-matrix.js"
      via: "provider capabilities and inactive/unsupported diagnostics"
      pattern: "capability|provider"
    - from: "marketing-tools.js"
      to: "command-router.js"
      via: "structured scaffold failure propagation"
      pattern: "provider|error|code"
---

<objective>
Add config-gated scaffold adapters for Gemini/OpenCode/Mistral, integrated into routing/install surfaces without changing shared runtime behavior.

Purpose: Establish ADPT-04/05/06 readiness scaffolds with explicit capability-gap signaling.
Output: scaffold provider modules, activation-gate wiring, and structured unsupported diagnostics.
</objective>

<execution_context>
@/home/ubuntu/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/ubuntu/marketing/.planning/PROJECT.md
@/home/ubuntu/marketing/.planning/ROADMAP.md
@/home/ubuntu/marketing/.planning/REQUIREMENTS.md
@/home/ubuntu/marketing/.planning/STATE.md
@/home/ubuntu/marketing/.planning/phases/07-additional-adapter-scaffolds/07-CONTEXT.md
@/home/ubuntu/marketing/.planning/phases/07-additional-adapter-scaffolds/07-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add minimal scaffold provider modules mapped to canonical actions</name>
  <files>/home/ubuntu/marketing/scripts/adapters/providers/gemini.js, /home/ubuntu/marketing/scripts/adapters/providers/opencode.js, /home/ubuntu/marketing/scripts/adapters/providers/mistral.js</files>
  <behavior>
    - Each scaffold provider exports immutable provider metadata and minimal native command map.
    - Minimal map covers critical command subset only.
    - Command actions use canonical action constants.
  </behavior>
  <action>
Create scaffold provider modules using existing provider module pattern (`provider`, `commandMap`) and canonical actions import.

Ensure native command prefixes and command names align with chosen scaffold surface policy.
  </action>
  <verify>
<automated>
node -e "['gemini','opencode','mistral'].forEach(p=>{const m=require('./scripts/adapters/providers/'+p); if(!m.provider||!m.commandMap||Object.keys(m.commandMap).length===0) throw new Error(p);}); console.log('ok');"
</automated>
  </verify>
  <done>
Scaffold providers exist as contract-compliant modules with minimal command maps.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register scaffolds with config-gated activation and structured unsupported diagnostics</name>
  <files>/home/ubuntu/marketing/scripts/adapters/command-router.js, /home/ubuntu/marketing/scripts/adapters/capability-matrix.js, /home/ubuntu/marketing/scripts/marketing-tools.js, /home/ubuntu/marketing/config.example.json</files>
  <behavior>
    - Router recognizes scaffold providers but blocks inactive providers with deterministic structured errors.
    - Unsupported scaffold capability paths emit `code`, `capability`, `remediation` fields.
    - Existing Claude/Codex behavior remains unchanged.
  </behavior>
  <action>
Extend router/provider registry and capability matrix integration to include scaffold provider activation gate logic.

Introduce config schema defaults for scaffold activation flags in `config.example.json` and ensure runtime reads these flags without altering default provider behavior.

Propagate structured scaffold errors through marketing-tools command execution output.
  </action>
  <verify>
<automated>
GMD_PROVIDER=gemini node scripts/marketing-tools.js '/gmd:campaign-progress' >/tmp/p7-gem.out 2>/tmp/p7-gem.err || true; node -e "const fs=require('fs'); const j=JSON.parse(fs.readFileSync('/tmp/p7-gem.err','utf8')); const ok=j.code&&j.remediation; console.log(ok); if(!ok) process.exit(1);"
</automated>
  </verify>
  <done>
Scaffold providers are safely dormant by default with machine-readable unsupported diagnostics.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add non-invasive installer scaffold registration hooks</name>
  <files>/home/ubuntu/marketing/bin/install.js</files>
  <action>
Update installer conversion/registration paths to account for scaffold provider command artifacts and activation hints without introducing full provider UX flows.

Keep installer behavior backward-compatible for existing global/local install workflows.
  </action>
  <verify>
<automated>
node -e "const fs=require('fs');const t=fs.readFileSync('./bin/install.js','utf8'); const ok=/gemini|opencode|mistral/.test(t); console.log(ok); if(!ok) process.exit(1);"
</automated>
  </verify>
  <done>
Installer includes scaffold registration hooks consistent with phase scope.
  </done>
</task>

</tasks>

<verification>
1. Scaffold provider modules load and expose minimal command maps.
2. Scaffold providers are config-gated and inactive by default.
3. Inactive/unsupported paths return structured machine-readable diagnostics.
4. Existing Claude/Codex routing behavior remains unchanged.
</verification>

<success_criteria>
- ADPT-04/05/06 scaffolds are contract-compliant and safely registered.
- Capability gaps are explicit, machine-enforced, and actionable.
- Plan 07-02 can validate scaffold conformance through scripted checks.
</success_criteria>

<output>
After completion, create `.planning/phases/07-additional-adapter-scaffolds/07-01-SUMMARY.md`
</output>
