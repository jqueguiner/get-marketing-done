---
phase: 06-parity-test-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/ubuntu/marketing/scripts/verify_adapter_parity.js
  - /home/ubuntu/marketing/scripts/verify_claude_parity.js
  - /home/ubuntu/marketing/scripts/verify_codex_command_sweep.js
  - /home/ubuntu/marketing/scripts/verify_cross_adapter_continuity.js
  - /home/ubuntu/marketing/scripts/verify_quality_gate_consistency.js
autonomous: true
requirements: ["QUAL-01", "QUAL-02"]

must_haves:
  truths:
    - "A single orchestrator runner executes parity sections sequentially and fails fast when a section fails."
    - "Each section result is tagged with mapped requirement IDs and emitted in machine-readable JSON."
    - "The parity harness composes existing validators rather than duplicating core continuity/gate logic."
  artifacts:
    - path: "/home/ubuntu/marketing/scripts/verify_adapter_parity.js"
      provides: "Top-level parity orchestrator with requirement-tagged section aggregation"
      min_lines: 130
    - path: "/home/ubuntu/marketing/scripts/verify_claude_parity.js"
      provides: "Normalized output contract consumable by orchestrator"
      min_lines: 120
    - path: "/home/ubuntu/marketing/scripts/verify_codex_command_sweep.js"
      provides: "Section output contract alignment for orchestrator"
      min_lines: 70
  key_links:
    - from: "verify_adapter_parity.js"
      to: "verify_claude_parity.js"
      via: "section invocation and result ingestion"
      pattern: "section|requirements|status"
    - from: "verify_adapter_parity.js"
      to: "verify_cross_adapter_continuity.js"
      via: "continuity section integration"
      pattern: "continuity|CMD-01|QUAL-02"
    - from: "verify_adapter_parity.js"
      to: "verify_quality_gate_consistency.js"
      via: "gate-consistency section integration"
      pattern: "QUAL-01|QUAL-02"
---

<objective>
Build the phase-6 parity orchestrator that unifies validator execution and emits requirement-mapped mismatch diagnostics for Claude/Codex compatibility checks.

Purpose: Establish QUAL-01/QUAL-02 detection capability with a deterministic JSON reporting contract.
Output: `verify_adapter_parity.js` orchestrator and normalized downstream validator interfaces.
</objective>

<execution_context>
@/home/ubuntu/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/ubuntu/marketing/.planning/PROJECT.md
@/home/ubuntu/marketing/.planning/ROADMAP.md
@/home/ubuntu/marketing/.planning/REQUIREMENTS.md
@/home/ubuntu/marketing/.planning/STATE.md
@/home/ubuntu/marketing/.planning/phases/06-parity-test-harness/06-CONTEXT.md
@/home/ubuntu/marketing/.planning/phases/06-parity-test-harness/06-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement top-level adapter parity orchestrator</name>
  <files>/home/ubuntu/marketing/scripts/verify_adapter_parity.js</files>
  <behavior>
    - Runs parity sections in deterministic sequence with fail-fast behavior.
    - Each section includes requirement tags (`CMD-01`, `QUAL-01`, `QUAL-02`) in report payload.
    - Emits JSON summary with section statuses, checks, and mismatches.
  </behavior>
  <action>
Create `scripts/verify_adapter_parity.js` (Node/CommonJS, stdlib only) that executes section scripts as child processes and aggregates outputs.

Initial sections should include:
- codex native command sweep,
- Claude parity validator,
- cross-adapter continuity validator,
- quality gate consistency validator.

Define a stable output contract including: `status`, `sections[]`, `requirements`, `failures[]`, and non-zero exit on first failed section.
  </action>
  <verify>
<automated>
node scripts/verify_adapter_parity.js
</automated>
  </verify>
  <done>
Orchestrated parity run is executable, requirement-tagged, and fail-fast.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize downstream validator result contracts for orchestration</name>
  <files>/home/ubuntu/marketing/scripts/verify_claude_parity.js, /home/ubuntu/marketing/scripts/verify_codex_command_sweep.js, /home/ubuntu/marketing/scripts/verify_cross_adapter_continuity.js, /home/ubuntu/marketing/scripts/verify_quality_gate_consistency.js</files>
  <behavior>
    - Downstream scripts expose consistent top-level fields consumed by orchestrator.
    - Existing script semantics remain backward-compatible for direct invocation.
    - Requirement tags can be attached without breaking script-specific details.
  </behavior>
  <action>
Adjust validators as needed so each script returns parseable JSON with explicit `status` and check detail arrays compatible with orchestrator ingestion.

Preserve existing command behavior and avoid removing script-specific diagnostic richness.
  </action>
  <verify>
<automated>
node scripts/verify_codex_command_sweep.js >/tmp/p6-sweep.json && node scripts/verify_claude_parity.js >/tmp/p6-claude.json && node scripts/verify_cross_adapter_continuity.js >/tmp/p6-continuity.json && node scripts/verify_quality_gate_consistency.js >/tmp/p6-gates.json && node -e "const fs=require('fs');['/tmp/p6-sweep.json','/tmp/p6-claude.json','/tmp/p6-continuity.json','/tmp/p6-gates.json'].forEach(p=>{const j=JSON.parse(fs.readFileSync(p,'utf8')); if(!j.status) throw new Error('missing status in '+p);}); console.log('ok');"
</automated>
  </verify>
  <done>
Underlying validators provide a stable machine-readable contract for parity orchestration.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add requirement-mapped mismatch diagnostics</name>
  <files>/home/ubuntu/marketing/scripts/verify_adapter_parity.js</files>
  <action>
Enhance orchestrator output to map every section failure and mismatch to one or more requirement IDs and include concise expected/actual excerpts for strict-field differences.

Ensure aggregate output can directly feed phase verification evidence in Plan 06-02.
  </action>
  <verify>
<automated>
node scripts/verify_adapter_parity.js | node -e "let s='';process.stdin.on('data',d=>s+=d);process.stdin.on('end',()=>{const j=JSON.parse(s); const ok=Array.isArray(j.sections)&&j.sections.every(sec=>Array.isArray(sec.requirements)); console.log(ok); if(!ok) process.exit(1);});"
</automated>
  </verify>
  <done>
Parity failures are requirement-tagged and actionable for fast triage.
  </done>
</task>

</tasks>

<verification>
1. A single harness command executes parity sections in deterministic order.
2. Section outputs are machine-readable and include requirement mappings.
3. Mismatches are reported with actionable field-level detail.
4. Harness exits non-zero on any section mismatch.
</verification>

<success_criteria>
- QUAL-01 instrumentation exists to detect and localize command/state mismatches.
- QUAL-02 coverage path is established through orchestrated critical-flow checks.
- Plan 06-02 can add CMD-01 flow assertions and phase verification evidence using this harness.
</success_criteria>

<output>
After completion, create `.planning/phases/06-parity-test-harness/06-01-SUMMARY.md`
</output>
