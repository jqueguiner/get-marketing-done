---
phase: 05-verification-gate-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/ubuntu/marketing/scripts/marketing-tools.js
  - /home/ubuntu/marketing/scripts/adapters/command-router.js
autonomous: true
requirements: ["SAFE-02"]

must_haves:
  truths:
    - "All send-adjacent actions execute the same runtime quality-gate evaluator before command execution continues."
    - "Gate policy is read from config.json quality_gates at execution time, with backward-compatible defaults via config-ensure."
    - "Gate failures return a stable machine-readable block contract with remediation guidance and no provider-specific divergence."
  artifacts:
    - path: "/home/ubuntu/marketing/scripts/marketing-tools.js"
      provides: "Shared quality-gate evaluator and uniform blocked response handling for gated actions"
      min_lines: 320
    - path: "/home/ubuntu/marketing/scripts/adapters/command-router.js"
      provides: "Provider-normalized metadata routing surface consumed by gate checks"
      min_lines: 100
  key_links:
    - from: "marketing-tools.js"
      to: "config.json"
      via: "quality_gates policy evaluation"
      pattern: "quality_gates|manual_verify_before_send"
    - from: "marketing-tools.js"
      to: "command-router.js"
      via: "provider normalization for identical adapter behavior"
      pattern: "provider|normalize"
    - from: "marketing-tools.js"
      to: "verify() output"
      via: "blocked remediation payload references"
      pattern: "verify|QUALITY_GATE_BLOCKED"
---

<objective>
Centralize verification and send-safety gate authority in shared runtime command execution so Claude and Codex enforce identical policy semantics.

Purpose: Close SAFE-02 by removing adapter-dependent gate behavior and making runtime enforcement deterministic.
Output: Shared evaluator in marketing runtime, explicit gated-action inventory, and stable blocked-response contract.
</objective>

<execution_context>
@/home/ubuntu/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/ubuntu/marketing/.planning/PROJECT.md
@/home/ubuntu/marketing/.planning/ROADMAP.md
@/home/ubuntu/marketing/.planning/REQUIREMENTS.md
@/home/ubuntu/marketing/.planning/STATE.md
@/home/ubuntu/marketing/.planning/phases/05-verification-gate-consistency/05-CONTEXT.md
@/home/ubuntu/marketing/.planning/phases/05-verification-gate-consistency/05-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add centralized runtime quality-gate evaluator for send-adjacent actions</name>
  <files>/home/ubuntu/marketing/scripts/marketing-tools.js</files>
  <behavior>
    - Gate policy evaluation runs at command execution time for every gated action.
    - Gated action inventory includes canonical outreach send-adjacent paths (`outreach.prepare`, `outreach.upload`) and their mapped runtime commands.
    - Existing command surfaces remain unchanged while enforcement is centralized.
  </behavior>
  <action>
Refactor `scripts/marketing-tools.js` to introduce a single helper (for example `evaluateQualityGates`) that accepts action/command context, config, and current runtime state.

Wire this helper into command dispatch before executing gated commands so both native and canonical-routed invocations receive identical checks.

Ensure policy defaults are sourced from `configEnsure()` and runtime reads from current `config.json` each invocation.
  </action>
  <verify>
<automated>
node -e "const fs=require('fs');const t=fs.readFileSync('./scripts/marketing-tools.js','utf8');const ok=/quality.?gate/i.test(t)&&/manual_verify_before_send/.test(t);console.log(ok);if(!ok)process.exit(1);"
</automated>
  </verify>
  <done>
Shared gate evaluation exists and is invoked for every send-adjacent runtime command.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Standardize blocked response payload and remediation fields</name>
  <files>/home/ubuntu/marketing/scripts/marketing-tools.js</files>
  <behavior>
    - All gate failures emit a stable payload code (e.g., `QUALITY_GATE_BLOCKED`).
    - Payload includes failed gate IDs and actionable remediation guidance.
    - Payload shape is identical across Claude and Codex invocations.
  </behavior>
  <action>
Implement a shared blocked-response constructor in `scripts/marketing-tools.js` and route all gate-denied paths through it.

Include fields for `code`, `failed_gates`, `remediation`, and relevant action/command context. Keep response JSON-only and backward-compatible for existing consumers.
  </action>
  <verify>
<automated>
node -e "const cp=require('child_process');const env={...process.env,GMD_PROVIDER:'codex'};const out=cp.spawnSync('node',['scripts/marketing-tools.js','outreach.upload'],{env,encoding:'utf8'});const txt=(out.stdout||'')+(out.stderr||'');const ok=/QUALITY_GATE_BLOCKED|failed_gates|remediation/.test(txt);console.log(ok);if(!ok)process.exit(1);"
</automated>
  </verify>
  <done>
Gate-blocked responses are deterministic, machine-readable, and provider-agnostic.
  </done>
</task>

<task type="auto">
  <name>Task 3: Ensure provider routing does not bypass centralized enforcement</name>
  <files>/home/ubuntu/marketing/scripts/marketing-tools.js, /home/ubuntu/marketing/scripts/adapters/command-router.js</files>
  <action>
Audit routing path from provider-native commands and canonical aliases to runtime command execution, then ensure gate checks happen after route resolution and before command handler execution.

Add or adjust lightweight metadata propagation needed for uniform enforcement diagnostics without introducing provider-specific branches.
  </action>
  <verify>
<automated>
GMD_PROVIDER=claude node scripts/marketing-tools.js /gmd:outreach.upload >/tmp/p5-claude.json 2>/tmp/p5-claude.err || true; GMD_PROVIDER=codex node scripts/marketing-tools.js '$gmd-upload' >/tmp/p5-codex.json 2>/tmp/p5-codex.err || true; rg -n "QUALITY_GATE_BLOCKED|UNKNOWN|failed_gates|remediation" /tmp/p5-claude.json /tmp/p5-claude.err /tmp/p5-codex.json /tmp/p5-codex.err
</automated>
  </verify>
  <done>
No provider route can execute a gated action without passing shared runtime gate enforcement.
  </done>
</task>

</tasks>

<verification>
1. Gated send-adjacent actions are evaluated by one shared runtime gate function.
2. `quality_gates.manual_verify_before_send` is enforced at execution time for both Claude and Codex.
3. Gate-blocked responses contain stable code, failed gate list, and actionable remediation fields.
4. Route resolution path does not permit adapter-specific bypass.
</verification>

<success_criteria>
- SAFE-02 enforcement authority is centralized in shared runtime logic.
- Claude and Codex produce equivalent gate outcomes and payload contracts for blocked actions.
- Downstream validator (Plan 05-02) can assert compliance deterministically.
</success_criteria>

<output>
After completion, create `.planning/phases/05-verification-gate-consistency/05-01-SUMMARY.md`
</output>
