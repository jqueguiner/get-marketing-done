---
phase: 04-cross-adapter-state-continuity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/ubuntu/marketing/scripts/marketing-tools.js
  - /home/ubuntu/marketing/scripts/adapters/command-router.js
autonomous: true
requirements: ["CMD-02", "SAFE-03"]

must_haves:
  truths:
    - "Resume resolution uses locked precedence with timestamp conflict handling across adapters."
    - "Corrupt/missing top resume source falls back with structured warning, not silent fail."
    - "Provider provenance metadata is tracked as backward-compatible additive fields."
  artifacts:
    - path: "/home/ubuntu/marketing/scripts/marketing-tools.js"
      provides: "Deterministic cross-adapter resume resolver and provenance state writes"
      min_lines: 260
    - path: "/home/ubuntu/marketing/scripts/adapters/command-router.js"
      provides: "Provider context propagation surface for continuity metadata"
      min_lines: 110
  key_links:
    - from: "marketing-tools.js"
      to: "data/.continue-here.md"
      via: "resume source parsing and timestamp arbitration"
      pattern: "continue|resume|paused"
    - from: "marketing-tools.js"
      to: "data/STATE.md"
      via: "frontmatter provenance writes"
      pattern: "last_provider|paused_by_provider"
    - from: "command-router.js"
      to: "marketing-tools.js"
      via: "provider metadata consistency"
      pattern: "provider"
---

<objective>
Implement the cross-adapter continuity contract in runtime state handling: deterministic resume source-of-truth, structured fallback warnings, and additive provenance metadata.

Purpose: Establish CMD-02 behavior guarantees and SAFE-03 compatibility baseline before scenario-level validation.
Output: Updated runtime/state logic for pause/resume/progress continuity across provider switches.
</objective>

<execution_context>
@/home/ubuntu/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/ubuntu/marketing/.planning/PROJECT.md
@/home/ubuntu/marketing/.planning/ROADMAP.md
@/home/ubuntu/marketing/.planning/REQUIREMENTS.md
@/home/ubuntu/marketing/.planning/STATE.md
@/home/ubuntu/marketing/.planning/phases/04-cross-adapter-state-continuity/04-CONTEXT.md
@/home/ubuntu/marketing/.planning/phases/04-cross-adapter-state-continuity/04-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic resume source resolver with timestamp arbitration</name>
  <files>/home/ubuntu/marketing/scripts/marketing-tools.js</files>
  <behavior>
    - Resume source order is `.continue-here.md` > `STATE.md` > pipeline snapshot.
    - Newer timestamp wins when continue/state artifacts disagree.
    - Tie or unparseable timestamp falls back to precedence order.
  </behavior>
  <action>
Refactor resume initialization logic in `scripts/marketing-tools.js` to introduce a single resolver function used by `initResume`.

Resolver behavior must:
- parse continue/state timestamps defensively,
- choose source using locked freshness + precedence rules,
- expose selected source and reason in returned payload for downstream diagnostics.

Keep existing command interface stable and preserve current output fields unless adding backward-compatible metadata.
  </action>
  <verify>
<automated>
node -e "const t=require('fs').readFileSync('./scripts/marketing-tools.js','utf8'); const ok=/resume|continue/i.test(t) && /timestamp/i.test(t); console.log(ok); if(!ok) process.exit(1);"
</automated>
  </verify>
  <done>
Resume resolution is deterministic, timestamp-aware, and precedence-compliant.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement structured fallback warnings and additive provenance metadata</name>
  <files>/home/ubuntu/marketing/scripts/marketing-tools.js</files>
  <behavior>
    - Missing/corrupt top source triggers structured warning payload.
    - Pause/resume writes `last_provider` and `paused_by_provider` without schema breakage.
    - Existing state files without new fields continue to parse and operate.
  </behavior>
  <action>
Update pause/resume/state mutation paths so provenance fields are written when provider context is known (via `GMD_PROVIDER`, defaulting safely).

Add structured warning objects for fallback events (for example: `warning_code`, `from`, `to`, `reason`) returned in resume payload.

Keep frontmatter parsing backward-compatible and do not require new fields for successful reads.
  </action>
  <verify>
<automated>
GMD_PROVIDER=codex node scripts/marketing-tools.js pause "phase4 continuity probe" >/tmp/p4-pause.json && node scripts/marketing-tools.js state-get last_provider >/tmp/p4-last.json && node -e "const a=require('/tmp/p4-pause.json'); const b=require('/tmp/p4-last.json'); const ok=typeof b.last_provider!=='undefined'; console.log(ok); if(!ok) process.exit(1);"
</automated>
  </verify>
  <done>
Fallback behavior is observable and provenance metadata persists without breaking existing schemas.
  </done>
</task>

<task type="auto">
  <name>Task 3: Expose continuity diagnostics needed by phase validator</name>
  <files>/home/ubuntu/marketing/scripts/marketing-tools.js, /home/ubuntu/marketing/scripts/adapters/command-router.js</files>
  <action>
Add lightweight diagnostics fields required by phase-4 continuity checks (selected resume source, fallback warning, provider provenance snapshot).

Diagnostics must be machine-readable and non-breaking for existing command consumers.
  </action>
  <verify>
<automated>
node scripts/marketing-tools.js init-resume | node -e "let s='';process.stdin.on('data',d=>s+=d);process.stdin.on('end',()=>{const j=JSON.parse(s); const ok=('suggested_action' in j)&&('has_continue_file' in j); console.log(ok); if(!ok) process.exit(1);});"
</automated>
  </verify>
  <done>
Continuity diagnostics are available for scripted cross-adapter validation in Plan 04-02.
  </done>
</task>

</tasks>

<verification>
1. Resume source is selected using locked precedence + timestamp rules.
2. Fallback from invalid top source emits structured warning payload.
3. `last_provider` and `paused_by_provider` are persisted additively.
4. Existing state files continue to parse without required new fields.
</verification>

<success_criteria>
- CMD-02 continuity contract implemented in runtime behavior.
- SAFE-03 upheld via additive-only state evolution.
- Plan 04-02 can validate cross-provider continuity scenarios deterministically.
</success_criteria>

<output>
After completion, create `.planning/phases/04-cross-adapter-state-continuity/04-01-SUMMARY.md`
</output>
