---
phase: 03-claude-adapter-parity-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/ubuntu/marketing/scripts/adapters/providers/claude.js
  - /home/ubuntu/marketing/scripts/adapters/command-router.js
  - /home/ubuntu/marketing/scripts/adapters/capability-matrix.js
  - /home/ubuntu/marketing/scripts/marketing-tools.js
autonomous: true
requirements: ["ADPT-02"]

must_haves:
  truths:
    - "Claude `/gmd:*` command set routes through the canonical adapter contract with no behavior regressions."
    - "Shared orchestration logic contains no Claude-only assumptions for command dispatch."
    - "Claude user-visible command behavior remains unchanged after refactor."
  artifacts:
    - path: "/home/ubuntu/marketing/scripts/adapters/providers/claude.js"
      provides: "Claude native command inventory mapped to canonical actions"
      min_lines: 25
    - path: "/home/ubuntu/marketing/scripts/adapters/command-router.js"
      provides: "Provider-agnostic routing path used by Claude commands"
      min_lines: 90
    - path: "/home/ubuntu/marketing/scripts/marketing-tools.js"
      provides: "Runtime route-then-execute path without Claude special-casing"
      min_lines: 220
  key_links:
    - from: "providers/claude.js"
      to: "command-router.js"
      via: "native command map lookup"
      pattern: "/gmd:"
    - from: "command-router.js"
      to: "marketing-tools.js"
      via: "canonical action to runtime command handoff"
      pattern: "routeCommand|ACTION_TO_COMMAND"
    - from: "capability-matrix.js"
      to: "command-router.js"
      via: "alias and conflict policy enforcement"
      pattern: "conflict_policy|alias"
---

<objective>
Refactor Claude integration onto the shared adapter contract boundary while preserving existing `/gmd:*` behavior and removing Claude-specific orchestration assumptions from shared core logic.

Purpose: Fulfill ADPT-02 by making Claude compatibility contract-driven, not branch-driven, with zero user-visible changes.
Output: Updated adapter/runtime wiring that is provider-agnostic and behavior-preserving for Claude commands.
</objective>

<execution_context>
@/home/ubuntu/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/ubuntu/marketing/.planning/PROJECT.md
@/home/ubuntu/marketing/.planning/ROADMAP.md
@/home/ubuntu/marketing/.planning/REQUIREMENTS.md
@/home/ubuntu/marketing/.planning/STATE.md
@/home/ubuntu/marketing/.planning/phases/03-claude-adapter-parity-refactor/03-CONTEXT.md
@/home/ubuntu/marketing/.planning/phases/03-claude-adapter-parity-refactor/03-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize Claude provider map against canonical action contract</name>
  <files>/home/ubuntu/marketing/scripts/adapters/providers/claude.js, /home/ubuntu/marketing/scripts/adapters/canonical-actions.js</files>
  <behavior>
    - Every `/gmd:*` Claude native command resolves to a valid canonical action.
    - No Claude native command key drifts from current published command surface.
    - Mapping changes do not introduce new user-visible command names.
  </behavior>
  <action>
Audit and refactor `scripts/adapters/providers/claude.js` so the command map is the single source of truth for Claude native command inventory and maps cleanly to canonical actions in `scripts/adapters/canonical-actions.js`.

Keep `/gmd:*` surface stable. If canonical action IDs are missing for an existing Claude command, add/align canonical constants without introducing provider-specific fields.
  </action>
  <verify>
<automated>
node -e "const p=require('./scripts/adapters/providers/claude.js'); const c=require('./scripts/adapters/canonical-actions.js'); const keys=Object.keys(p.commandMap); const vals=new Set(Object.values(c.ACTIONS)); const ok=keys.length>=16 && keys.every(k=>k.startsWith('/gmd:')) && keys.every(k=>vals.has(p.commandMap[k])); console.log(ok); if(!ok) process.exit(1);"
</automated>
  </verify>
  <done>
Claude provider map is contract-complete and stable: all native commands are `/gmd:*` and map to valid canonical actions.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove Claude-specific dispatch assumptions from shared runtime path</name>
  <files>/home/ubuntu/marketing/scripts/adapters/command-router.js, /home/ubuntu/marketing/scripts/adapters/capability-matrix.js, /home/ubuntu/marketing/scripts/marketing-tools.js</files>
  <behavior>
    - Shared routing behavior is provider-agnostic and policy-driven.
    - Claude commands route via adapter contract before execution when appropriate.
    - Unknown-command behavior remains stable for Claude users.
  </behavior>
  <action>
Refactor `scripts/adapters/command-router.js` and `scripts/marketing-tools.js` to ensure Claude command handling uses the same route-then-execute architecture as other providers, without introducing Claude-only special branches in shared orchestration.

Use `capability-matrix.js` for policy lookups rather than hard-coded provider assumptions where applicable. Preserve current user-facing error/output behavior for Claude native commands.
  </action>
  <verify>
<automated>
node -e "const {routeCommand}=require('./scripts/adapters/command-router'); const r=routeCommand({provider:'claude',command:'/gmd:campaign-progress',config:{aliases:false}}); const ok=r && r.action==='campaign.progress'; console.log(ok); if(!ok) process.exit(1);"
</automated>
<automated>
GMD_PROVIDER=claude node scripts/marketing-tools.js /gmd:campaign-progress >/tmp/gmd-claude-progress.json && node -e "const fs=require('fs'); const j=JSON.parse(fs.readFileSync('/tmp/gmd-claude-progress.json','utf8')); const ok=typeof j.progress_pct==='number' && typeof j.current_step!=='undefined'; console.log(ok); if(!ok) process.exit(1);"
</automated>
  </verify>
  <done>
Claude native commands execute through shared adapter contract paths with unchanged user-visible behavior.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add refactor safety checks for Claude command inventory and dispatch links</name>
  <files>/home/ubuntu/marketing/scripts/adapters/command-router.js, /home/ubuntu/marketing/scripts/marketing-tools.js</files>
  <action>
Add lightweight diagnostics/helpers needed by downstream parity validation to enumerate Claude supported commands and confirm route-to-runtime linkage is intact.

Keep helpers minimal, non-invasive, and suitable for use in an automated parity validator in Plan 03-02.
  </action>
  <verify>
<automated>
node -e "const r=require('./scripts/adapters/command-router.js'); const cmds=r.listSupportedCommands('claude'); const ok=Array.isArray(cmds)&&cmds.length>=16&&cmds.every(c=>c.startsWith('/gmd:')); console.log(ok); if(!ok) process.exit(1);"
</automated>
  </verify>
  <done>
Shared runtime surfaces expose enough deterministic Claude routing diagnostics for parity gating.
  </done>
</task>

</tasks>

<verification>
1. Claude native command map remains stable (`/gmd:*`) and canonical-action backed.
2. Claude command execution reaches runtime through shared adapter contract paths.
3. No net new Claude-only orchestration assumptions exist in shared core routing path.
4. User-visible Claude behavior is unchanged for existing command surface.
</verification>

<success_criteria>
- ADPT-02 implementation is in place at adapter/runtime boundary.
- Claude behavior preservation is enforced while refactoring internals.
- Plan 03-02 can consume new diagnostics for parity regression gating.
</success_criteria>

<output>
After completion, create `.planning/phases/03-claude-adapter-parity-refactor/03-01-SUMMARY.md`
</output>
