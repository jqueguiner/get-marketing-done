---
phase: 03-claude-adapter-parity-refactor
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - /home/ubuntu/marketing/scripts/verify_claude_parity.js
  - /home/ubuntu/marketing/README.md
  - /home/ubuntu/marketing/.planning/phases/03-claude-adapter-parity-refactor/03-VERIFICATION.md
autonomous: true
requirements: ["ADPT-02", "CMD-03"]

must_haves:
  truths:
    - "Phase 3 parity baseline is pinned to last tagged release and enforced by automation."
    - "`progress` and `verify` flows are checked with hybrid parity rules (strict key fields + semantic non-critical checks)."
    - "Any Claude parity regression fails verification and blocks phase completion."
  artifacts:
    - path: "/home/ubuntu/marketing/scripts/verify_claude_parity.js"
      provides: "Automated Claude parity validator with tagged-baseline comparison"
      min_lines: 100
    - path: "/home/ubuntu/marketing/.planning/phases/03-claude-adapter-parity-refactor/03-VERIFICATION.md"
      provides: "Phase-level verification record for ADPT-02 and CMD-03"
      min_lines: 60
    - path: "/home/ubuntu/marketing/README.md"
      provides: "Operator-visible parity validation command and expectations"
      contains: "verify_claude_parity"
  key_links:
    - from: "verify_claude_parity.js"
      to: "providers/claude.js"
      via: "Claude native command inventory baseline checks"
      pattern: "commandMap"
    - from: "verify_claude_parity.js"
      to: "marketing-tools.js"
      via: "runtime output parity checks for progress/verify"
      pattern: "scripts/marketing-tools.js"
    - from: "03-VERIFICATION.md"
      to: "REQUIREMENTS.md"
      via: "ADPT-02 and CMD-03 closure evidence"
      pattern: "ADPT-02|CMD-03"
---

<objective>
Implement and document a deterministic Claude parity gate that compares current behavior against the last release tag, with hybrid strict/semantic assertions for `progress` and `verify` outputs.

Purpose: Close CMD-03 and finalize ADPT-02 confidence by enforcing the user-locked regression policy.
Output: `verify_claude_parity.js` plus phase verification artifacts proving no regressions.
</objective>

<execution_context>
@/home/ubuntu/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/ubuntu/marketing/.planning/PROJECT.md
@/home/ubuntu/marketing/.planning/ROADMAP.md
@/home/ubuntu/marketing/.planning/REQUIREMENTS.md
@/home/ubuntu/marketing/.planning/STATE.md
@/home/ubuntu/marketing/.planning/phases/03-claude-adapter-parity-refactor/03-CONTEXT.md
@/home/ubuntu/marketing/.planning/phases/03-claude-adapter-parity-refactor/03-RESEARCH.md
@/home/ubuntu/marketing/.planning/phases/03-claude-adapter-parity-refactor/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build tagged-baseline Claude parity validator</name>
  <files>/home/ubuntu/marketing/scripts/verify_claude_parity.js</files>
  <behavior>
    - Baseline tag resolves deterministically (default latest tag).
    - Validator compares Claude command surface against baseline.
    - `progress` and `verify` outputs use hybrid strict/semantic comparison rules.
    - Validator exits non-zero on any regression.
  </behavior>
  <action>
Create `scripts/verify_claude_parity.js` (Node/CommonJS, stdlib only) that:
- Resolves baseline from latest git tag (override via env/arg allowed).
- Captures baseline snapshots for Claude command map and runtime outputs for:
  - `/gmd:campaign-progress`
  - `/gmd:campaign-verify`
- Captures current snapshots at HEAD for the same checks.
- Compares with hybrid rules:
  - strict fields (e.g., `current_step`, `current_step_name`, `progress_pct`, `status`, `score_pct`) must match exactly.
  - non-critical fields can be semantically compared (ordering/messages tolerated when intent is unchanged).
- Emits machine-readable summary and exits 1 on regressions.

Do not change codex policies or codex validators in this task.
  </action>
  <verify>
<automated>
node scripts/verify_claude_parity.js
</automated>
  </verify>
  <done>
Claude parity validator is reproducible, baseline-pinned, and fail-fast on regressions.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce progress/verify parity checks in phase verification artifacts</name>
  <files>/home/ubuntu/marketing/.planning/phases/03-claude-adapter-parity-refactor/03-VERIFICATION.md</files>
  <behavior>
    - Verification file records ADPT-02 and CMD-03 evidence from validator output.
    - Fail state is explicit when any regression is detected.
  </behavior>
  <action>
Create or update `03-VERIFICATION.md` to include explicit checks for:
- Claude command-surface parity vs baseline tag.
- Progress output parity (strict + semantic sections).
- Verify output parity (strict + semantic sections).
- Requirement closure statements for ADPT-02 and CMD-03.

Use the validator output as evidence source and keep status deterministic (`passed` only when zero regressions).
  </action>
  <verify>
<automated>
rg -n "ADPT-02|CMD-03|progress|verify|status:" .planning/phases/03-claude-adapter-parity-refactor/03-VERIFICATION.md
</automated>
  </verify>
  <done>
Phase verification artifact explicitly proves parity gate success for ADPT-02 and CMD-03.
  </done>
</task>

<task type="auto">
  <name>Task 3: Document Claude parity gate usage and blocking policy</name>
  <files>/home/ubuntu/marketing/README.md</files>
  <action>
Update README compatibility/validation docs to include:
- `node scripts/verify_claude_parity.js` command.
- Baseline rule (last tagged release by default).
- Hybrid strict/semantic parity policy for `progress` and `verify`.
- Blocking behavior on regressions.

Keep wording consistent with locked phase decisions and existing Codex validation documentation style.
  </action>
  <verify>
<automated>
rg -n "verify_claude_parity|tagged release|strict|semantic|regression" README.md
</automated>
  </verify>
  <done>
README reflects the enforced Claude parity gate and operator expectations.
  </done>
</task>

</tasks>

<verification>
1. `verify_claude_parity.js` fails on introduced regressions and passes on parity.
2. Baseline tag and comparison scope are explicit in output.
3. Progress/verify checks apply hybrid strict/semantic assertions.
4. Verification artifact and docs reflect blocker policy for regressions.
</verification>

<success_criteria>
- ADPT-02 verified against tagged baseline with no regressions.
- CMD-03 satisfied with explicit progress/verify parity evidence.
- Phase exit can be blocked automatically if parity drifts.
</success_criteria>

<output>
After completion, create `.planning/phases/03-claude-adapter-parity-refactor/03-02-SUMMARY.md`
</output>
