---
phase: 01-canonical-contracts
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - /home/ubuntu/marketing/scripts/adapters/command-router.js
  - /home/ubuntu/marketing/scripts/adapters/providers/claude.js
  - /home/ubuntu/marketing/scripts/adapters/providers/codex.js
  - /home/ubuntu/marketing/scripts/marketing-tools.js
  - /home/ubuntu/marketing/README.md
autonomous: true
requirements: ["ADPT-03", "SAFE-01"]

must_haves:
  truths:
    - "Native assistant commands can be translated into canonical actions without changing existing command behavior."
    - "Codex and Claude provider maps are isolated modules that do not expose provider auth into core action routing."
    - "Alias handling is opt-in and respects native-precedence conflict policy."
  artifacts:
    - path: "/home/ubuntu/marketing/scripts/adapters/command-router.js"
      provides: "Native command to canonical action routing entrypoint"
      min_lines: 60
    - path: "/home/ubuntu/marketing/scripts/adapters/providers/claude.js"
      provides: "Claude-native command map for router"
      min_lines: 30
    - path: "/home/ubuntu/marketing/scripts/adapters/providers/codex.js"
      provides: "Codex-native command map for router"
      min_lines: 30
    - path: "/home/ubuntu/marketing/scripts/marketing-tools.js"
      provides: "Core tool bootstrap uses canonical action routing path"
      contains: "routeCommand"
    - path: "/home/ubuntu/marketing/README.md"
      provides: "Compatibility and alias usage note"
      contains: "canonical alias"
  key_links:
    - from: "command-router.js"
      to: "canonical-actions.js"
      via: "validate and normalize routed action payload"
      pattern: "assertValidAction|normalizeActionPayload"
    - from: "command-router.js"
      to: "capability-matrix.js"
      via: "provider capability lookup and alias policy check"
      pattern: "getProviderCapabilities|getAliasPolicy"
    - from: "marketing-tools.js"
      to: "command-router.js"
      via: "native command ingestion path calls router"
      pattern: "routeCommand"
---

<objective>
Integrate the canonical contract into runtime entrypoints by adding command router and provider maps, then route existing command invocations through canonical actions without breaking native behavior.

Purpose: This locks the Phase 1 contract into actual orchestration paths so future adapter phases build on a working boundary, not documentation only.
Output: Router + provider map modules, bootstrap integration in marketing-tools, and user docs describing alias model.
</objective>

<execution_context>
@/home/ubuntu/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/ubuntu/marketing/.planning/PROJECT.md
@/home/ubuntu/marketing/.planning/ROADMAP.md
@/home/ubuntu/marketing/.planning/REQUIREMENTS.md
@/home/ubuntu/marketing/.planning/STATE.md
@/home/ubuntu/marketing/.planning/phases/01-canonical-contracts/01-CONTEXT.md
@/home/ubuntu/marketing/.planning/phases/01-canonical-contracts/01-RESEARCH.md
@/home/ubuntu/marketing/.planning/phases/01-canonical-contracts/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement command router and provider maps</name>
  <files>/home/ubuntu/marketing/scripts/adapters/command-router.js, /home/ubuntu/marketing/scripts/adapters/providers/claude.js, /home/ubuntu/marketing/scripts/adapters/providers/codex.js</files>
  <behavior>
    - Routes provider-native commands to canonical actions through explicit map entries.
    - Honors alias policy: aliases disabled by default, syntax `gmd:<action>`, native wins on conflicts.
    - Keeps provider maps free of auth/config secrets.
  </behavior>
  <action>
Create `scripts/adapters/providers/` directory and add:
- `claude.js` command map for existing `/gmd:*` command names.
- `codex.js` command map for corresponding Codex-native invocation labels used in this project workflow.

Create `scripts/adapters/command-router.js`:
- Import helpers from `canonical-actions.js` and `capability-matrix.js`.
- Export `routeCommand({ provider, command, params = {}, config = {} })`.
- Flow:
  1. Load provider map.
  2. Resolve native command first.
  3. If not found and alias mode enabled in config, resolve canonical alias `gmd:<action>`.
  4. On native/alias conflict, prefer native and return metadata warning.
  5. Validate with `assertValidAction`, return normalized payload.
- Export `listSupportedCommands(provider)` helper for diagnostics/docs.
Keep provider-auth fields out of return payload.
  </action>
  <verify>
<automated>
node -e "const r=require('./scripts/adapters/command-router.js'); const out=r.routeCommand({provider:'claude',command:'/gmd:campaign-progress'}); console.log(out.action==='campaign.progress'); const a=r.routeCommand({provider:'codex',command:'gmd:campaign.progress',config:{aliases:true}}); console.log(a.action==='campaign.progress');"
</automated>
  </verify>
  <done>
Router and provider map modules exist and route known native commands plus opt-in aliases according to locked policy.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate router into marketing-tools ingress path</name>
  <files>/home/ubuntu/marketing/scripts/marketing-tools.js</files>
  <behavior>
    - Command ingress can use canonical routing path without breaking current command behavior.
    - Unknown commands return clear structured errors.
    - Core init/state/outreach logic remains unchanged.
  </behavior>
  <action>
Update `scripts/marketing-tools.js` to consume `routeCommand` for command ingress:
- Add adapter import near top-level requires.
- In command parsing/dispatch section, translate incoming provider/native command to canonical action before executing handlers.
- Preserve backward compatibility by mapping existing CLI command inputs to same downstream functions.
- On unsupported command, return structured JSON error with `provider`, `command`, and supported command list.
- Ensure existing init commands and current outputs remain stable.
  </action>
  <verify>
<automated>
node scripts/marketing-tools.js init-campaign | head -n 1 >/dev/null; echo "init-ok" && node -e "const r=require('./scripts/adapters/command-router.js'); console.log(Array.isArray(r.listSupportedCommands('claude')));"
</automated>
  </verify>
  <done>
Marketing tools use canonical command router at ingress while preserving existing operational behavior.
  </done>
</task>

<task type="auto">
  <name>Task 3: Document compatibility and alias policy</name>
  <files>/home/ubuntu/marketing/README.md</files>
  <action>
Update README with a concise section describing:
- provider-native command behavior remains primary,
- canonical aliases are optional and disabled by default,
- alias syntax is `gmd:<action>`,
- conflict behavior: native commands take precedence and alias path warns.
Add one short example for Claude-native and one for canonical alias mode.
Do not remove existing command tables; append compatibility note in an appropriate architecture/usage section.
  </action>
  <verify>
<automated>
rg -n "canonical alias|gmd:<action>|native commands take precedence|Codex" README.md
</automated>
  </verify>
  <done>
README contains clear multi-assistant command compatibility guidance aligned with locked decisions.
  </done>
</task>

</tasks>

<verification>
1. Router/provider modules load and resolve known commands successfully.
2. Marketing tools command ingress continues to function for existing flows.
3. Alias mode is opt-in and conflict policy is native-precedence.
4. No provider auth/config fields are required in canonical action routing payloads.
5. README documents canonical alias policy and examples.
</verification>

<success_criteria>
- Canonical contracts are consumed by runtime ingress, not just documented.
- Provider command maps are isolated and extensible for later adapter phases.
- Locked command naming decisions are implemented exactly (opt-in aliases, `gmd:<action>`, native precedence).
- SAFE-01 boundary remains intact across routing code.
</success_criteria>

<output>
After completion, create `.planning/phases/01-canonical-contracts/01-02-SUMMARY.md`
</output>
