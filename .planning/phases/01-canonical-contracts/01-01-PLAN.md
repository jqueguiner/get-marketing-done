---
phase: 01-canonical-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/ubuntu/marketing/scripts/adapters/canonical-actions.js
  - /home/ubuntu/marketing/scripts/adapters/capability-matrix.js
  - /home/ubuntu/marketing/docs/architecture/canonical-actions.md
  - /home/ubuntu/marketing/docs/architecture/assistant-capability-matrix.md
autonomous: true
requirements: ["ADPT-03", "SAFE-01"]

must_haves:
  truths:
    - "Maintainers can reference one canonical action contract for workflow intents across assistants."
    - "Maintainers can define and update assistant capability metadata from a single matrix module and document."
    - "Core contract artifacts do not require provider-specific auth/config details."
  artifacts:
    - path: "/home/ubuntu/marketing/scripts/adapters/canonical-actions.js"
      provides: "Canonical action constants and validation helpers for adapter routing"
      min_lines: 40
    - path: "/home/ubuntu/marketing/scripts/adapters/capability-matrix.js"
      provides: "Provider capability schema and lookup helpers"
      min_lines: 40
    - path: "/home/ubuntu/marketing/docs/architecture/canonical-actions.md"
      provides: "Human-readable canonical action contract and field definitions"
      contains: "Canonical Actions"
    - path: "/home/ubuntu/marketing/docs/architecture/assistant-capability-matrix.md"
      provides: "Provider capability matrix, constraints, and conflict policy"
      contains: "Capability Matrix"
  key_links:
    - from: "canonical-actions.js"
      to: "assistant-capability-matrix.md"
      via: "Action names and categories documented to match exported constants"
      pattern: "campaign.progress|work.pause|outreach.prepare"
    - from: "capability-matrix.js"
      to: "assistant-capability-matrix.md"
      via: "Provider keys and capability fields aligned with documented matrix"
      pattern: "supports"
---

<objective>
Define the canonical contract artifacts for multi-assistant operation: action schema and provider capability matrix, with explicit documentation and reusable modules.

Purpose: Phase 1 requires a shared contract foundation before adapter implementations. This plan establishes the source of truth for action vocabulary and assistant capabilities while keeping provider auth/config isolated from core logic.
Output: Canonical action and capability modules plus architecture docs that downstream plans and adapters can consume directly.
</objective>

<execution_context>
@/home/ubuntu/.codex/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/ubuntu/marketing/.planning/PROJECT.md
@/home/ubuntu/marketing/.planning/ROADMAP.md
@/home/ubuntu/marketing/.planning/REQUIREMENTS.md
@/home/ubuntu/marketing/.planning/STATE.md
@/home/ubuntu/marketing/.planning/phases/01-canonical-contracts/01-CONTEXT.md
@/home/ubuntu/marketing/.planning/phases/01-canonical-contracts/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create canonical action contract module</name>
  <files>/home/ubuntu/marketing/scripts/adapters/canonical-actions.js</files>
  <behavior>
    - Exposes a stable set of canonical action identifiers for current GTM workflow intents.
    - Rejects unknown/invalid action names through a validation helper.
    - Does not include assistant/provider-specific auth fields in contract shape.
  </behavior>
  <action>
Create `scripts/adapters/canonical-actions.js` as a CommonJS module with:
- Frozen `ACTIONS` constant object (initial intents: campaign progress/verify, work pause/resume, list/enrichment/email/outreach actions currently represented in commands).
- `isValidAction(action)` helper returning boolean.
- `assertValidAction(action)` helper that throws a clear error for unknown actions.
- `normalizeActionPayload({ action, params, metadata })` helper that:
  - validates action using `assertValidAction`
  - returns `{ action, params: params || {}, metadata: metadata || {} }`
  - strips provider-auth fields if present (`token`, `api_key`, `access_token`, `secret`) to enforce SAFE-01 boundary.
- Export all helpers via `module.exports`.
Add a short module header comment documenting that provider auth/config belongs in adapter/provider modules only.
  </action>
  <verify>
<automated>
node -e "const c=require('./scripts/adapters/canonical-actions.js'); console.log(Object.keys(c.ACTIONS).length>5 && c.isValidAction('campaign.progress')); c.assertValidAction('campaign.progress'); const n=c.normalizeActionPayload({action:'campaign.progress',params:{},metadata:{token:'x'}}); console.log(!('token' in n.metadata));"
</automated>
  </verify>
  <done>
Canonical action module exists, exports constants and validators, validates known actions, and enforces boundary by removing provider-auth fields from normalized metadata.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create provider capability matrix module</name>
  <files>/home/ubuntu/marketing/scripts/adapters/capability-matrix.js</files>
  <behavior>
    - Provides one shared provider capability matrix for all adapters.
    - Supports provider lookup and default/fallback behavior for unsupported providers.
    - Reflects conflict policy: native command precedence with alias warning strategy.
  </behavior>
  <action>
Create `scripts/adapters/capability-matrix.js` as a CommonJS module with:
- `CAPABILITY_MATRIX` frozen object with entries for at least `claude`, `codex`, `gemini`, `opencode`, `mistral`.
- Per provider fields:
  - `native_command_prefixes` (array),
  - `supports` object (`inline_options`, `hooks`, `statusline`, `interactive_checkpoints`, `alias_mode`),
  - `conflict_policy` set to `native_wins_alias_warns`.
- `getProviderCapabilities(provider)` helper returning provider entry or `null`.
- `assertProviderSupported(provider)` helper with clear error.
- `getAliasPolicy(provider)` returning `{ enabled_by_default: false, syntax: 'gmd:<action>', conflict_policy: 'native_wins_alias_warns' }`.
Ensure no secret/auth values are represented in this matrix.
Export matrix and helpers.
  </action>
  <verify>
<automated>
node -e "const m=require('./scripts/adapters/capability-matrix.js'); console.log(!!m.getProviderCapabilities('claude')); console.log(m.getAliasPolicy('codex').syntax==='gmd:<action>'); console.log(m.getProviderCapabilities('claude').conflict_policy==='native_wins_alias_warns');"
</automated>
  </verify>
  <done>
Capability matrix exists with required providers and helpers, and alias/conflict policy aligns with locked decisions.
  </done>
</task>

<task type="auto">
  <name>Task 3: Document canonical contract and capability matrix</name>
  <files>/home/ubuntu/marketing/docs/architecture/canonical-actions.md, /home/ubuntu/marketing/docs/architecture/assistant-capability-matrix.md</files>
  <action>
Create `docs/architecture/` directory if missing. Add:
- `canonical-actions.md` documenting:
  - purpose and scope of canonical actions,
  - action naming conventions,
  - payload fields,
  - explicit boundary that provider auth/config is out of core contract scope.
- `assistant-capability-matrix.md` documenting:
  - provider capability table from matrix module,
  - alias model (`gmd:<action>`, opt-in),
  - conflict policy (`native_wins_alias_warns`),
  - guidance for adding new providers.
Reference corresponding source modules in each document.
  </action>
  <verify>
<automated>
rg -n "Canonical Actions|provider auth|gmd:<action>|native_wins_alias_warns" docs/architecture/canonical-actions.md docs/architecture/assistant-capability-matrix.md
</automated>
  </verify>
  <done>
Both architecture docs exist and are aligned with modules and locked phase decisions.
  </done>
</task>

</tasks>

<verification>
1. `node -e "require('./scripts/adapters/canonical-actions.js'); require('./scripts/adapters/capability-matrix.js'); console.log('ok')"` executes successfully.
2. Canonical actions and capability matrix exports are available and non-empty.
3. Docs contain canonical alias syntax `gmd:<action>` and conflict policy `native_wins_alias_warns`.
4. No provider secret fields are part of canonical contract or capability matrix modules.
</verification>

<success_criteria>
- Shared canonical action contract exists and is reusable by adapters.
- Shared capability matrix exists with provider metadata for current and target assistants.
- Contract docs define boundaries clearly and preserve SAFE-01.
- Locked decisions from CONTEXT.md are encoded in matrix/doc policy.
</success_criteria>

<output>
After completion, create `.planning/phases/01-canonical-contracts/01-01-SUMMARY.md`
</output>
